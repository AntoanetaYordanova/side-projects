<section id='details-page'>
  <h1>Details</h1>
  <article class='details-card'>

    <article class='details-card-text'>
      <h2>Title:<br /> {{data.title}}</h2>
      <h3>Author: {{data.author}}</h3>
      <h3>Painting technique: {{data.technique}}</h3>
      <h3>Certificate of authenticity: {{data.certificate}}</h3>

      {{#if hasUser}}
        <div class='buttons'>
          {{#if userData.isOwner}}
            <a href='/edit/{{data._id}}' class='btn-edit'>Edit</a>
            <a href='/delete/{{data._id}}' class='btn-delete'>Delete</a>
          {{else}}
            {{#if userData.hasShared}}
              <p class='shared-pub'>You already shared this publication</p>
            {{else}}
              <a href='/share/{{data._id}}' class='btn-share'>Share publication</a>
            {{/if}}
            
          {{/if}}
        </div>
      {{/if}}
    </article>

    <article class='details-card-image'>
      <img src='{{data.picture}}' alt='art-image2' />
    </article>

  </article>
</section>